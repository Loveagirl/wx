<!--components/navi/index.wxml-->
<view class="container">
    <image class="icon" src="{{ Latest ? disLeft : left}}" bind:tap="onLeft" />
    <text class="title">{{title}}</text>
    <image class="icon" src="{{First ? disRight : right}}" bind:tap="onRight"  />    
</view>
